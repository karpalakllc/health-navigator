---
description: Zdravje360 monorepo — read brief, architecture, and TASKS before structural changes
alwaysApply: true
---

# Zdravje360 project rules

## Required reading

Before proposing **structural changes** (new apps, packages, top-level folders, cross-cutting patterns, auth architecture, database engine choice, or API/client boundaries):

1. Read [PROJECT_BRIEF.md](../../PROJECT_BRIEF.md)
2. Read [docs/architecture.md](../../docs/architecture.md)
3. Read [TASKS.md](../../TASKS.md) and [docs/roadmap.md](../../docs/roadmap.md); follow the **current active roadmap phase** only (today: **R1 launch** — beta verification + ops; MVP is complete unless reprioritized)

If a change contradicts those documents, call it out and suggest updating the docs first.

## Repository map

- `apps/api` — Laravel 13 API, Filament admin (when installed), PostgreSQL, jobs, Meilisearch indexing
- `apps/web` — Next.js public frontend, Macedonian-first UI
- `docs/` — architecture and design
- `infra/` — deployment definitions (future)
- `packages/` — reserved; do not add shared code here unless duplication is proven and agreed in `TASKS.md`

Do not add new top-level applications without an explicit task in `TASKS.md`.

## Architecture constraints

- **Direct API:** Next.js calls Laravel over HTTPS JSON by default. Do not introduce a Next.js BFF/proxy layer unless the user requests it for a documented exception (e.g. SSR session aggregation).
- **API-first:** Business logic, validation, and persistence live in Laravel. The web app is a client.
- **Honest scaffold:** Distinguish what exists (skeleton) from target stack (PostgreSQL, Redis, Meilisearch, Filament). Do not imply features are shipped when they are only planned.
- **Locale:** Macedonian is the primary launch language; English is optional later.

## What not to build without approval

- New domain features outside the active roadmap phase (see TASKS **R1 launch**, then **R2–R8**, gated **G** for AI triage)
- Meilisearch, sponsorships, mobile — until the relevant TASKS roadmap phase (R3+)
- Symptom guidance **visual rule builder** — post-MVP (validation/helper text shipped in MVP-4)
- Composer/npm **dependencies** — do not add without user approval; align with the active phase and existing stack versions

## Monorepo discipline

- **Scope:** Keep edits in one app (`apps/api` or `apps/web`) unless a cross-app change is required; avoid drive-by changes in the other app.
- **Roadmap phase:** **MVP is complete.** Active work is **R1 launch** (beta verification, staging deploy). Do not start **R2+** or **G (3f-b AI)** unless the user reprioritizes.
- **Permissions:** Moderators **view** directory/guidance/categories; **admins** mutate directory/triage/categories. UGC moderation is **staff** — see TASKS MVP-1 table.

## Implementation habits

- Scope edits to the correct app (`apps/api` vs `apps/web`).
- Prefer small, reviewable changes tied to one phase or epic.
- When finishing a task, briefly state what changed, what was intentionally left unchanged, and any assumptions.
- When code exists: follow Laravel and Next.js conventions in each app; run existing linters/tests.
- Medical UX: never present platform content as diagnosis or emergency care; include disclaimers for triage-related work.
- Sponsored or featured content must be clearly labeled in UI copy when implemented.

## Documentation

- If architecture, workflow, or phase scope changes, update the relevant docs (**`docs/architecture.md`**, **`TASKS.md`**, or **`PROJECT_BRIEF.md`**) in the **same change** — especially the architecture decision log for binding technical choices.
- Keep [README.md](../../README.md) as the main entry point; link outward rather than duplicating long sections.
